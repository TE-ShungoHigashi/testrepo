name: call composite action

on:
  workflow_dispatch:
    inputs:
       exit:
         type: choice
         options: ["0", "1"]
         default: "0"

jobs:
  call_action:
    runs-on: ubuntu-latest
    steps:
      - run: |
          test=$(echo '${{runner.name}}')
          echo $test
          echo '${{runner.os}}'
      - run: pwd && ls -l
      - working-directory: /home/runner
        run: mkdir test && echo 'hogehoge' >> test/text.txt && ls -lR
      - uses: actions/checkout@v4
        with:
          path: ~/test
      - working-directory: ~/
        run: ls -lR .
      - name: step A
        run: sleep 20; exit ${{ inputs.exit }}
      - name: step B
        if: always()
        uses: ./.github/actions/tmp1
      
