name: call composite action

on:
  workflow_dispatch:

jobs:
  call_action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: step A
        run: sleep 20; exit 1
      - name: step B
        if: always()
        uses: ./.github/actions/tmp1
      
