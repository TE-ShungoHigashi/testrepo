name: call composite action

on:
  workflow_dispatch:
    inputs:
      field:
        type: choice
        options: ["A", "B"]
      injection:
        type: string

jobs:
  call_action:
    runs-on: ubuntu-latest
    env:
      test: hoge
    steps:
      - run: |
          sleep 30
          exit 1
      - if: always()
        run: echo always
      - if: ${{ ! cancelled() }}
        run: echo not cancelled
