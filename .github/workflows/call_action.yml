name: call composite action

on:
  workflow_dispatch:
    inputs:
      field:
        type: choice
        options: ["A", "B"]
      injection:
        type: string

jobs:
  call_action:
    runs-on: ubuntu-latest
    env:
      test: hoge
    steps:
      - continue-on-error: true
        run: exit 1
