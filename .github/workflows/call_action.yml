name: call composite action

on:
  workflow_dispatch:
    inputs:
      field:
        type: choice
        options: ["A", "B"]

jobs:
  call_action:
    runs-on: ubuntu-latest
    env:
      test: hoge
    steps:
      - id: twice_output
        run: |
          echo "val=hoge" >> $GITHUB_OUTPUT
          echo "val=piyo" >> $GITHUB_OUTPUT
      - run: echo "${{steps.twice_output.outputs.val}}" 
      - uses: actions/checkout@v4
      - uses: ./.github/actions/tmp1
