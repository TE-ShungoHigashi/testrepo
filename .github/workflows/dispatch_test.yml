name: dispatch_test
on:
  workflow_dispatch:
    inputs:
      val:
        required: false
        type: string
env:
  default_val: hogepiyo
  HOGE: hohoho

jobs:
  job1:
    name: '[HELP] help me'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{inputs.val || env.default_val}}
      - uses: actions/checkout@v4
      - run: ./test.sh
      - run: printenv
