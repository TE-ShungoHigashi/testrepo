name: dispatch_test
on:
  workflow_dispatch:
    inputs:
      labels:
        description: labels
        required: true
        type: string

jobs:
  job1:
    if: ${{inputs.labels != ''}}
    runs-on: ${{inputs.labels}}
    steps:
      - run: |
          echo ${{inputs.mode}}
    
