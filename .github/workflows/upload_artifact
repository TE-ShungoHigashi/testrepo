name: upload artifact

on:
  push:

jobs:
  job1:
  runs-on: ubuntu-latest
  steps:
    - run: actions/checkout@v4
    - run: actions/upload-artifact@v4
      with:
        name: this_is_name
        path: test.sh
    
